//Logic behind having a default sort on End score with Descending being on
//Change id to any column accessor name to make default sort of the table from that particular column

export const DEFAULT_SORT = {
	id: "end_score",
	desc: true,
};

export const BROKER_SORT = {
	id: "per_trade",
};
